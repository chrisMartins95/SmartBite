/* ============================================================
🧩 LEGACY FILE: Sidebar
===============================================================
Este arquivo representa uma versão **antiga/simplificada**
da sidebar que foi substituída pela nova `AppSidebar` 
(moderno e responsivo).

💡 Ele existe apenas para evitar erros em imports antigos
— ou seja, serve como *compat layer* (camada de compatibilidade).
=========================================================== */

// 🚫 Nenhuma exportação real — tudo foi migrado para AppSidebar
export {};
