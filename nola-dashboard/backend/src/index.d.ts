/* 
=========================================================
🧩 Declaração de Módulo Vazio — index.d.ts
=========================================================
Este arquivo existe apenas para que o TypeScript reconheça
o diretório atual como um módulo.

📘 Contexto:
- O `export {}` cria um módulo “vazio” válido no TypeScript.
- Isso evita erros do tipo “This file is not a module”.
- É útil quando o arquivo é apenas um ponto de entrada 
  (ex: exportações automáticas ou geração de tipos).

⚙️ O arquivo `.map` referenciado é apenas para debugging.
*/

// 🧱 Exportação vazia — define o arquivo como um módulo
export {};

// 🗺️ Mapa de origem TypeScript — usado apenas para debugging
//# sourceMappingURL=index.d.ts.map
