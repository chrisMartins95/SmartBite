/* 
=========================================================
🧭 Declaração das Rotas do Dashboard
=========================================================
Este arquivo `.d.ts` (TypeScript Declaration File) serve 
para informar ao TypeScript que existe uma constante 
`router` exportada em `dashboardRoutes.ts`.

Essa constante representa o roteador principal do Express 
que agrupa todas as rotas relacionadas ao dashboard 
(métricas, vendas, tendências, etc).
*/

// 📦 Exporta o router (instância de Router do Express)
export declare const router: import("express-serve-static-core").Router;

// 🗺️ Mapa de origem TypeScript — usado apenas para debugging
//# sourceMappingURL=dashboardRoutes.d.ts.map
